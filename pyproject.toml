[project]
name = "nl-hope-pytorch"
version = "0.1.0"
description = "Nested Learning (NL) and HOPE in PyTorch"
requires-python = ">=3.9"
dependencies = [
  "torch>=2.2",
  "transformers>=4.45",
  "datasets>=2.19",
  "sentencepiece>=0.1.99",
  "tqdm>=4.66",
  "numpy>=1.25",
  "pyyaml>=6.0.1",
]
